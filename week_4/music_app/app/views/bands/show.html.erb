<h1><%= @band.name %></h1>

<h3>Albums</h3>
<ul>
  <% @albums = @band.albums %>
  <%= render @albums %>
</ul>

<ul>
  <li><%= link_to "Add Album", new_band_album_url(@band) %></li>
  <li><%= link_to "Edit Band", edit_band_url(@band) %></li>
  <li><%= button_to "Delete Band", band_url(@band), method: :delete %></li>
  <li><%= link_to "View All Bands", bands_url %></li>
</ul>
